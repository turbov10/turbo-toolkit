[pytest]
testpaths = tests
addopts = -v --tb=short --strict-markers
markers =
    vision: tests that require the macOS Vision backend (only run on darwin)
    rapidocr: tests that require the rapidocr-onnxruntime backend
